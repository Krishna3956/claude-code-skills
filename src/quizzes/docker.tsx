import type { QuizConfig } from "@/components/quiz/types";

export const dockerConfig: QuizConfig = {
  slug: "docker",
  toolName: "Docker",
  tagline: "6 rounds. ~3 min. No account required.",
  subtitle: "Just you vs. Docker trivia.",
  sansFont: "inter",
  serifFont: "source-serif-4",
  accentColor: "#1D63ED",
  accentColorDim: "#1554CC",
  accentColorSubtle: "rgba(29,99,237,0.08)",
  bgColor: "#F5F8FF",
  bgElevated: "#FFFFFF",
  bgSurface: "#FFFFFF",
  bgSurfaceLight: "#E5EEFF",
  textColor: "#17191E",
  textSecondary: "#3D4555",
  textTertiary: "#7A8599",
  borderColor: "#C8D5E8",
  borderSubtle: "#E5EDF5",
  scorecardBg: "#17191E",
  scorecardDivider: "#2A2E38",
  scorecardLabelColor: "#7A8599",
  scorecardGridColor: "#2A2E38",
  logo: (<img src="/logos/docker.svg" alt="Docker" width={44} height={44} style={{ borderRadius: 10 }} />),
  analyticsPrefix: "docker",
  dimensionLabels: {
    memory: "Images & Containers",
    orchestration: "Compose & Orchestration",
    automation: "Networking & Volumes",
    extensibility: "Registry & CI/CD",
    workflows: "Advanced Features",
    prompting: "Dockerfile Craft",
    bestPractices: "Best Practices",
  },
  archetypes: [
    { title: "Container Architect", emoji: "🏗️", description: "You've mastered Docker at every level. Multi-stage builds, orchestration, security hardening, and production deployments are your domain.", minScore: 90, maxScore: 100 },
    { title: "Compose Commander", emoji: "🎼", description: "You orchestrate multi-container applications with ease. Networking, volumes, and build optimization are second nature.", minScore: 80, maxScore: 89 },
    { title: "Image Optimizer", emoji: "📦", description: "You build efficient images and understand layers. A few more advanced techniques and you'll be a Docker expert.", minScore: 70, maxScore: 79 },
    { title: "Docker Runner", emoji: "🏃", description: "You run containers and use basic commands well, but there's a whole world of optimization and orchestration ahead.", minScore: 60, maxScore: 69 },
    { title: "Pull & Runner", emoji: "🐳", description: "You know docker pull and docker run. Time to discover what makes Docker a complete platform.", minScore: 50, maxScore: 59 },
    { title: "Tutorial Follower", emoji: "📖", description: "You've followed a Docker tutorial and got something running. The real understanding is coming.", minScore: 40, maxScore: 49 },
    { title: "First Container", emoji: "🌱", description: "Welcome to Docker. You just discovered that 'it works on my machine' is a solvable problem.", minScore: 0, maxScore: 39 },
  ],
  challenges: [
    { type: "truth_or_myth", id: 1, dimension: "prompting", statement: "Multi-stage Docker builds let you use one stage to compile your code and a separate, minimal stage for the final image, dramatically reducing image size by leaving build tools behind", isTrue: true, explanation: "Multi-stage builds use multiple FROM statements. Compile in a full SDK image, then COPY only the binary into a tiny runtime image (like distroless or alpine). A Go app can go from 1GB to 10MB." },
    { type: "truth_or_myth", id: 2, dimension: "orchestration", statement: "Docker Compose can define and run multi-container applications with networking, volumes, environment variables, and health checks, all from a single YAML file", isTrue: true, explanation: "docker-compose.yml defines your entire application stack: web server, database, cache, message queue. One 'docker compose up' starts everything with proper networking, volume mounts, and dependency ordering." },
    { type: "truth_or_myth", id: 3, dimension: "memory", statement: "Each line in a Dockerfile creates a new layer in the image, and Docker caches these layers. Changing one line invalidates the cache for that line and everything after it", isTrue: true, explanation: "Docker's layer caching is key to fast builds. Put rarely-changing instructions (like COPY package.json and RUN npm install) before frequently-changing ones (like COPY . .). This way, dependency installation is cached." },
    { type: "truth_or_myth", id: 4, dimension: "automation", statement: "Docker containers share the host machine's kernel. They are NOT virtual machines and do not run their own operating system", isTrue: true, explanation: "Containers use Linux namespaces and cgroups for isolation, sharing the host kernel. This is why they start in milliseconds (no OS boot) and use far less memory than VMs. Isolation without the overhead." },

    { type: "this_or_that", id: 5, dimension: "bestPractices", scenario: "You want to run your application as a non-root user inside the container for security. What's the best approach?", optionA: "Add a USER instruction in the Dockerfile to switch to a non-root user", optionB: "Run the container with --privileged flag for better isolation", correct: "A", explanation: "The USER instruction switches to a non-root user. The --privileged flag does the opposite: it gives the container full access to the host. Always run as non-root and never use --privileged unless absolutely necessary." },
    { type: "this_or_that", id: 6, dimension: "prompting", scenario: "Your Docker image is 2GB and takes forever to build. What's the most impactful optimization?", optionA: "Use a multi-stage build with a minimal base image like alpine or distroless", optionB: "Add more RUN commands to organize the Dockerfile better", correct: "A", explanation: "Multi-stage builds and minimal base images are the biggest wins. Alpine Linux is ~5MB vs Ubuntu's ~70MB. Distroless images contain only your app and its runtime dependencies. More RUN commands actually add more layers." },
    { type: "this_or_that", id: 7, dimension: "automation", scenario: "You need your database container's data to persist even when the container is removed and recreated.", optionA: "Use a Docker volume to store the data outside the container", optionB: "Never remove the container so the data stays", correct: "A", explanation: "Docker volumes persist data independently of container lifecycle. Mount a volume to /var/lib/postgresql/data and your database survives container removal, updates, and recreation." },

    { type: "quick_pick", id: 8, dimension: "memory", scenario: "You want to see what's happening inside a running container, check logs, and troubleshoot issues. What command lets you get a shell inside the container?", blank: "docker exec -it container_name /bin/sh", options: ["docker exec -it container_name /bin/sh", "docker ssh container_name", "docker connect container_name"], explanation: "docker exec -it opens an interactive terminal session inside a running container. Combined with docker logs for output and docker inspect for configuration, you have a complete debugging toolkit." },
    { type: "quick_pick", id: 9, dimension: "extensibility", scenario: "You want to share your Docker image with your team without setting up a private registry. What's the simplest approach?", blank: "Push to Docker Hub or use docker save to export as a tar file", options: ["Push to Docker Hub or use docker save to export as a tar file", "Copy the Dockerfile and rebuild on each machine", "Docker images cannot be shared"], explanation: "Docker Hub provides free public and private repositories. For offline sharing, 'docker save' exports an image to a tar file and 'docker load' imports it. Both methods preserve all layers and metadata." },
    { type: "quick_pick", id: 10, dimension: "bestPractices", scenario: "You want to prevent your Docker build from including unnecessary files like node_modules, .git, and local config files. What file controls this?", blank: ".dockerignore", options: [".dockerignore", ".gitignore (Docker reads it automatically)", "Dockerfile EXCLUDE instruction"], explanation: ".dockerignore works like .gitignore but for Docker builds. List patterns for files and directories to exclude from the build context. This speeds up builds and prevents sensitive files from ending up in images." },

    { type: "speed_pick", id: 11, dimension: "orchestration", prompt: "Tap every real Docker command!", correctItems: ["docker build", "docker compose up", "docker exec", "docker volume create", "docker network ls"], wrongItems: ["docker deploy", "docker cluster", "docker scale", "docker migrate"], timeLimit: 15, explanation: "build, compose up, exec, volume create, and network ls are all real Docker commands. deploy, cluster, scale (standalone), and migrate are not standard Docker CLI commands." },
    { type: "speed_pick", id: 12, dimension: "prompting", prompt: "Tap every valid Dockerfile instruction!", correctItems: ["FROM", "COPY", "RUN", "EXPOSE", "HEALTHCHECK"], wrongItems: ["IMPORT", "INSTALL", "LISTEN", "REQUIRE"], timeLimit: 15, explanation: "FROM, COPY, RUN, EXPOSE, and HEALTHCHECK are all valid Dockerfile instructions. IMPORT, INSTALL, LISTEN, and REQUIRE are not Dockerfile instructions." },

    { type: "odd_one_out", id: 13, dimension: "automation", prompt: "One of these is NOT a Docker networking mode. Which one?", items: ["bridge", "host", "none", "mesh"], oddItem: "mesh", explanation: "Bridge (default), host (share host network), and none (no networking) are all Docker network modes. Mesh networking is a concept from Docker Swarm overlay networks, not a standalone network mode." },
    { type: "odd_one_out", id: 14, dimension: "bestPractices", prompt: "One of these is NOT a real Docker Desktop feature. Which one?", items: ["Docker Scout (vulnerability scanning)", "Docker Build Cloud", "Docker Debug", "Docker AutoScale"], oddItem: "Docker AutoScale", explanation: "Docker Scout scans for vulnerabilities, Build Cloud speeds up builds, and Debug helps troubleshoot containers. Docker AutoScale is not a real feature. Autoscaling is handled by orchestrators like Kubernetes." },

    { type: "truth_or_myth", id: 15, dimension: "workflows", statement: "Docker BuildKit supports advanced features like cache mounts (persisting package manager caches between builds), secret mounts (using secrets during build without storing them in layers), and parallel stage execution in multi-stage builds", isTrue: true, explanation: "BuildKit is Docker's modern build engine. Cache mounts (--mount=type=cache) persist npm/pip caches between builds. Secret mounts (--mount=type=secret) inject secrets without baking them into layers. Parallel execution builds independent stages simultaneously." },
  ],
  rounds: [
    { name: "Truth or Myth", icon: "?", ids: [1, 2, 3, 4], tagline: "Real feature or total BS?" },
    { name: "This or That", icon: "vs", ids: [5, 6, 7], tagline: "Pick the smarter move" },
    { name: "Quick Pick", icon: ">>", ids: [8, 9, 10], tagline: "Name that feature" },
    { name: "Speed Round", icon: "::", ids: [11, 12], tagline: "Tap fast, think faster" },
    { name: "Odd One Out", icon: "//", ids: [13, 14], tagline: "Spot the fake" },
    { name: "Final Boss", icon: "*", ids: [15], tagline: "One shot. Expert level." },
  ],
};
